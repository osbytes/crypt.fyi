# Auth / registry settings only (pnpm 11+).
# All other install/security settings live in pnpm-workspace.yaml.
# https://pnpm.io/npmrc

registry=https://registry.npmjs.org/
@crypt.fyi:registry=https://registry.npmjs.org/
